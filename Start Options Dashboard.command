#!/bin/bash
#
# Double-click this file to start the Options Analysis Dashboard.
# A window opens and runs the app, then your browser opens to it automatically.
# To stop the dashboard later, close this window (or press Ctrl+C in it).
#
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/run.sh"
